export function IdentityChangeTimeline({observations}:{observations:{id:string;observation_type:string;severity:string;provider:string|null;observed_at:string}[]}){return <section className="surface rounded-xl p-5"><p className="eyebrow">Identity changes</p><h2 className="mt-1 text-lg font-semibold">Recent observations</h2><ol className="mt-4 space-y-3">{observations.length?observations.map(o=><li className="border-l border-white/10 pl-3 text-sm" key={o.id}><strong>{o.observation_type.replaceAll("_"," ")}</strong><p className="text-xs text-zinc-400">{o.provider??"identity"} · {o.severity} · {new Date(o.observed_at).toLocaleString()}</p></li>):<li className="text-sm text-zinc-400">No identity changes observed.</li>}</ol></section>}
