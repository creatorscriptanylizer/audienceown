import { emergencyApiContext, emergencyError } from "@/lib/emergency/api";
import { planSchema } from "@/lib/emergency/preparedness-schemas";

export async function GET(){const ctx=await emergencyApiContext();if(!ctx)return Response.json({error:"Unauthorized"},{status:401});const{data,error}=await ctx.client.from("emergency_plans").select("*,emergency_templates(name)").eq("creator_id",ctx.creator.id).order("updated_at",{ascending:false});return error?emergencyError(error):Response.json({plans:data},{headers:{"cache-control":"private, no-store"}});}
export async function POST(request:Request){const ctx=await emergencyApiContext();if(!ctx)return Response.json({error:"Unauthorized"},{status:401});let body;try{body=await request.json();}catch{return Response.json({error:"Invalid JSON"},{status:400});}const parsed=planSchema.safeParse(body);if(!parsed.success)return Response.json({error:"Invalid plan",fields:parsed.error.flatten().fieldErrors},{status:400});const{data,error}=await ctx.client.from("emergency_plans").insert({...parsed.data,creator_id:ctx.creator.id,created_by:ctx.user.id,readiness_status:"incomplete"}).select().single();return error?emergencyError(error):Response.json({plan:data},{status:201});}
