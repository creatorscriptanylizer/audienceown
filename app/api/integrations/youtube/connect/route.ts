import{randomBytes}from"node:crypto";import{cookies}from"next/headers";import{NextResponse}from"next/server";import{getCreator,getViewer}from"@/lib/dal";import{createYouTubeOAuthState,getYouTubeAuthorizationUrl}from"@/lib/youtube-oauth";import{createAdminClient}from"@/lib/supabase/admin";import{debugLog}from"@/lib/debug";import{socialTokenEncryptionState}from"@/lib/social-secrets";import{canCreateProviderConnection}from"@/lib/provider-entitlements";import{canonicalAccountConnected}from"@/lib/social-providers/connection-health";export const runtime="nodejs";export async function GET(request:Request){const[user,creator]=await Promise.all([getViewer(),getCreator()]);if(!user||!creator)return NextResponse.redirect(new URL("/login?next=/dashboard/platforms",request.url));const url=new URL(request.url);const roleValue=url.searchParams.get("role");const role=roleValue===null?"official":roleValue;const returnTo=url.searchParams.get("returnTo")==="onboarding"?"onboarding"as const:undefined;if(role!=="official"&&role!=="backup")return NextResponse.redirect(new URL("/dashboard/platforms?youtube=invalid_request",request.url));const encryptionState=socialTokenEncryptionState();if(encryptionState!=="configured"){debugLog("oauth",{area:"youtube_oauth",step:"preflight_configuration",status:"FAILED",social_token_encryption:encryptionState==="missing"?"MISSING":"INVALID FORMAT"});return NextResponse.redirect(new URL("/dashboard/platforms?youtube=not_configured",request.url));}const connectionId=url.searchParams.get("connectionId")??undefined;const protectedOfficialAccountId=url.searchParams.get("protectedOfficialAccountId")??undefined;if(connectionId){const admin=createAdminClient();if(!admin)return NextResponse.redirect(new URL("/dashboard/platforms?youtube=not_configured",request.url));const{data:connection,error:connectionError}=await admin.from("connected_accounts").select("id,account_type").eq("id",connectionId).eq("creator_id",creator.id).eq("platform","youtube").maybeSingle();if(connectionError)return NextResponse.redirect(new URL("/dashboard/platforms?youtube=temporarily_unavailable",request.url));if(!connection||connection.account_type!==role)return NextResponse.redirect(new URL("/dashboard/platforms?youtube=invalid_connection",request.url));}if(role==="backup"&&protectedOfficialAccountId){const admin=createAdminClient();if(!admin)return NextResponse.redirect(new URL("/dashboard/platforms?youtube=not_configured",request.url));const{data:official,error:officialError}=await admin.from("connected_accounts").select("id,url,external_account_id,connection_health,provider_status").eq("id",protectedOfficialAccountId).eq("creator_id",creator.id).eq("platform","youtube").eq("account_type","official").maybeSingle();if(officialError)return NextResponse.redirect(new URL("/dashboard/platforms?youtube=temporarily_unavailable",request.url));if(!official||!canonicalAccountConnected({health:official.connection_health,providerStatus:official.provider_status,hasPublicUrl:Boolean(official.url),hasExternalAccountId:Boolean(official.external_account_id)}))return NextResponse.redirect(new URL("/dashboard/platforms?youtube=invalid_official_account",request.url));}const entitlement=await canCreateProviderConnection(creator.id,role,connectionId?"reconnect":"new_connection",user);if(!entitlement.allowed)return NextResponse.redirect(new URL(returnTo?`/onboarding/accounts?step=${role}&oauth=youtube:connection_limit_reached`:"/dashboard/platforms?youtube=connection_limit_reached",request.url));if(process.env.NODE_ENV==="development"&&role==="backup"){console.info("youtube_backup_verify",{step:"oauth_start",connectionId:connectionId??null,role});}const nonce=randomBytes(24).toString("base64url");const state=createYouTubeOAuthState({creatorId:creator.id,userId:user.id,nonce,expiresAt:Date.now()+10*60_000,role,returnTo,...(connectionId?{connectionId}:{}),...(protectedOfficialAccountId?{protectedOfficialAccountId}:{})});const store=await cookies();store.set("youtube_oauth_nonce",nonce,{httpOnly:true,secure:process.env.NODE_ENV==="production",sameSite:"lax",path:"/api/integrations/youtube/callback",maxAge:600});return NextResponse.redirect(getYouTubeAuthorizationUrl(state));}
