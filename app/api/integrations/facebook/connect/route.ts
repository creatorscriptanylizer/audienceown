// Facebook uses the Meta asset-selection flow. This stable provider path keeps
// picker routing provider-neutral while preserving the existing Meta OAuth implementation.
export { GET } from "@/app/api/integrations/meta/connect/route";
