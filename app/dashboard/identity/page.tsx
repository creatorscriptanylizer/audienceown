import{requireCreator}from"@/lib/dal";import{createClient}from"@/lib/supabase/server";import{IdentityOverviewCard}from"@/components/identity/identity-overview-card";import{IdentityAccountCard}from"@/components/identity/identity-account-card";import{IdentityDomainCard}from"@/components/identity/identity-domain-card";import{IdentityRelationshipTimeline}from"@/components/identity/identity-relationship-timeline";import{IdentityHistory}from"@/components/identity/identity-history";
export default async function IdentityPage(){const creator=await requireCreator(),db=await createClient();const[{data:profile},{data:accounts},{data:domains},{data:relationships},{data:events}]=await Promise.all([db!.from("creator_identity_profiles").select("*").eq("creator_id",creator.id).maybeSingle(),db!.from("creator_identity_accounts").select("*").eq("creator_id",creator.id).order("provider"),db!.from("creator_identity_domains").select("*").eq("creator_id",creator.id).order("primary_domain",{ascending:false}),db!.from("creator_identity_relationships").select("id,relationship_type,status,created_at,source_account_id,target_account_id").eq("creator_id",creator.id).order("created_at",{ascending:false}),db!.from("creator_identity_events").select("id,event_type,source,created_at").eq("creator_id",creator.id).order("id",{ascending:false}).limit(30)]);const rows=accounts??[];return <div className="space-y-6"><header><p className="eyebrow">Creator identity graph</p><h1 className="mt-2 text-3xl font-semibold">Identity</h1><p className="mt-2 max-w-3xl text-sm text-zinc-400">One verified map of your official accounts, domains, replacements, and migrations. Handles may change; stable provider identity keeps continuity intact.</p></header><IdentityOverviewCard status={profile?.identity_status??"incomplete"} total={rows.length} verified={rows.filter(x=>x.verification_status==="verified").length} attention={rows.filter(x=>x.verification_status==="needs_attention").length} providers={new Set(rows.map(x=>x.provider)).size}/><section><p className="eyebrow">Accounts</p><h2 className="mt-2 text-xl font-semibold">Verified identity accounts</h2><div className="mt-4 grid gap-4 xl:grid-cols-2">{rows.length?rows.map(account=><IdentityAccountCard account={account} key={account.id}/>):<p className="surface rounded-xl p-5 text-sm text-zinc-400">Verified connected accounts will appear after identity synchronization.</p>}</div></section><section><p className="eyebrow">Domains</p><h2 className="mt-2 text-xl font-semibold">Verified domains</h2><div className="mt-4 grid gap-4 md:grid-cols-2">{(domains??[]).map(domain=><IdentityDomainCard domain={domain} key={domain.id}/>)}</div></section><div className="grid gap-6 xl:grid-cols-2"><IdentityRelationshipTimeline relationships={relationships??[]}/><IdentityHistory events={events??[]}/></div></div>}
