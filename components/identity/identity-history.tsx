export function IdentityHistory({events}:{events:Array<{id:number;event_type:string;source:string;created_at:string}>}){return <section className="surface rounded-xl p-5"><p className="eyebrow">Identity history</p><h2 className="mt-2 text-lg font-semibold">Recent changes</h2><div className="mt-4 divide-y divide-white/10">{events.length?events.map(event=><div className="py-3" key={event.id}><strong className="text-sm">{event.event_type.replaceAll("_"," ")}</strong><p className="text-xs text-zinc-500">{event.source.replaceAll("_"," ")} · {new Date(event.created_at).toLocaleString()}</p></div>):<p className="text-sm text-zinc-500">Identity synchronization has not recorded changes yet.</p>}</div></section>}
