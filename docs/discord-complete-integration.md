# Discord complete integration

Discord OAuth connects user identity and discovers manageable guilds. Bot installation, guild selection, and announcement-channel selection are separate operations. Only explicitly selected public announcement channels may enable detection, and every resulting draft requires approval. No arbitrary messages, private threads, member lists, role inventories, or channel history are collected. Public invite fallback remains unverified.
