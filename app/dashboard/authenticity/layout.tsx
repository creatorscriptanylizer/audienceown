import{requireCreator}from"@/lib/dal";import{createClient}from"@/lib/supabase/server";import{CopyLinkButton}from"@/components/copy-link-button";import{getPublicAuthenticity}from"@/lib/authenticity/server";import{buildAuthenticityManifest,domainDiscoveryDocument,serializeManifest}from"@/lib/authenticity/manifest";import{publicBaseUrl}from"@/lib/authenticity/public";
export default async function AuthenticityLayout({children}:{children:React.ReactNode}){const creator=await requireCreator(),record=await getPublicAuthenticity(creator.public_slug),db=await createClient(),{data:statements}=await db!.from("creator_continuity_statements").select("id,statement_type,provider,issued_at,revoked_at,superseded_at").eq("creator_id",creator.id).order("issued_at",{ascending:false}).limit(10),{data:subscriptions}=await db!.from("authenticity_network_subscriptions").select("id,endpoint_url,status,event_types,last_success_at,last_failure_at").eq("creator_id",creator.id).order("created_at",{ascending:false}),base=publicBaseUrl(),manifestUrl=`${base}/api/public/creators/${creator.public_slug}/manifest`,feedUrl=`${base}/api/public/creators/${creator.public_slug}/authenticity/events`,continuityUrl=`${base}/api/public/creators/${creator.public_slug}/continuity`,manifest=record?buildAuthenticityManifest(record):null,domainJson=manifest?JSON.stringify(domainDiscoveryDocument(manifest),null,2):"Unavailable";return <>{children}<section className="mt-6 space-y-6"><header><p className="eyebrow">Authenticity Network</p><h2 className="mt-2 text-2xl font-semibold">Portable identity proofs</h2><p className="mt-2 text-sm text-zinc-400">Publish manifests, continuity proofs, safe event feeds, webhooks, and SDK-verifiable records.</p></header><div className="grid gap-6 xl:grid-cols-2"><article className="surface rounded-2xl p-6"><h3 className="font-semibold">Public manifest and discovery</h3><div className="mt-4 flex flex-wrap gap-2"><CopyLinkButton value={manifestUrl} label="Copy manifest URL"/><CopyLinkButton value="/.well-known/audienceown.json" label="Copy well-known path"/><CopyLinkButton value={domainJson} label="Copy domain JSON"/></div>{manifest&&<pre className="mt-4 max-h-48 overflow-auto rounded-xl bg-black p-3 text-xs text-zinc-400">{serializeManifest(manifest)}</pre>}</article><article className="surface rounded-2xl p-6"><h3 className="font-semibold">Developer endpoints</h3><div className="mt-4 flex flex-wrap gap-2"><CopyLinkButton value={feedUrl} label="Copy event feed"/><CopyLinkButton value={continuityUrl} label="Copy continuity URL"/><CopyLinkButton value={`${base}/.well-known/audienceown-network.json`} label="Copy network discovery"/></div><pre className="mt-4 overflow-auto rounded-xl bg-black p-3 text-xs text-zinc-400">{`const client = new AudienceOwnAuthenticityClient({ issuer: "${base}" });\nconst manifest = await client.manifest("${creator.public_slug}");`}</pre></article></div><div className="grid gap-6 xl:grid-cols-2"><article className="surface rounded-2xl p-6"><h3 className="font-semibold">Continuity statements</h3><ul className="mt-4 space-y-2 text-sm">{(statements??[]).map(s=><li className="flex justify-between border-b border-white/10 pb-2" key={s.id}><a className="text-violet-300" href={`/api/public/creators/${creator.public_slug}/continuity/${s.id}`}>{s.statement_type.replaceAll("_"," ")} · {s.provider}</a><span className="text-zinc-500">{s.revoked_at?"Revoked":s.superseded_at?"Superseded":"Current"}</span></li>)}</ul></article><article className="surface rounded-2xl p-6"><h3 className="font-semibold">Webhook subscriptions</h3><p className="mt-2 text-xs text-zinc-500">Manage via `/api/developer/authenticity/subscriptions`. Secrets are shown only at creation or rotation.</p><ul className="mt-4 space-y-2 text-sm">{(subscriptions??[]).map(s=><li className="border-b border-white/10 pb-2" key={s.id}><span className="block break-all">{s.endpoint_url}</span><small className="text-zinc-500">{s.status} · {s.event_types.length} event types</small></li>)}</ul></article></div></section></>}
