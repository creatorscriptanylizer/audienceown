import{Activity,ShieldAlert}from"lucide-react";export function MonitoringSummary({open,recent,lastRun}:{open:number;recent:number;lastRun:string|null}){return <section className="surface rounded-xl p-5"><div className="flex items-center gap-3"><Activity className="text-sky-300"/><div><p className="eyebrow">Continuous monitoring</p><h2 className="mt-1 text-xl font-semibold">Identity monitoring health</h2></div></div><div className="mt-5 grid grid-cols-3 gap-3">{[["Open incidents",open],["Recent changes",recent],["Status",open?"Action needed":"Healthy"]].map(([label,value])=><div className="rounded-lg border border-white/10 p-3" key={label}><strong>{value}</strong><p className="text-xs text-zinc-400">{label}</p></div>)}</div><p className="mt-4 flex items-center gap-2 text-xs text-zinc-500"><ShieldAlert size={14}/> Provider-backed checks only. No follower alerts or emergency activation occur automatically. {lastRun?`Last observation ${new Date(lastRun).toLocaleString()}.`:"Monitoring has not reported yet."}</p></section>}
