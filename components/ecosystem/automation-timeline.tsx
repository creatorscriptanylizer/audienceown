export function AutomationTimeline({observations}:{observations:Array<{id:string;observation_type:string;provider:string;severity:string;observed_at:string}>}){return <section className="surface rounded-2xl p-6"><h2 className="font-semibold">Automation timeline</h2><ul className="mt-4 space-y-2 text-sm">{observations.length?observations.map(o=><li className="flex justify-between gap-4 border-b border-white/10 pb-2" key={o.id}><span>{o.provider} · {o.observation_type.replaceAll("_"," ")} <small className="text-zinc-500">({o.severity})</small></span><time className="text-zinc-500">{new Date(o.observed_at).toLocaleString()}</time></li>):<li className="text-zinc-500">No automation observations yet.</li>}</ul></section>}
