import Link from "next/link";
export default async function Page({params}:{params:Promise<{token:string}>}){const{token}=await params;let status="invalid";if(process.env.NEXT_PUBLIC_SUPABASE_URL)try{const r=await fetch(`${process.env.NEXT_PUBLIC_SUPABASE_URL}/functions/v1/unsubscribe`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({token}),cache:"no-store"});status=(await r.json()).status}catch{status="error"}const ok=status==="unsubscribed"||status==="already_unsubscribed";return <main className="grid min-h-screen place-items-center px-5 text-center"><div><p className="eyebrow">Recovery Pass</p><h1 className="mt-3 text-3xl font-semibold">{ok?"Recovery Pass deactivated":"This link isn’t valid"}</h1><p className="mt-3 text-zinc-400">{ok?"Alerts for this creator are now off. You can activate a new pass from their creator page.":"The private link could not be verified."}</p><Link href="/" className="button button-secondary mt-7">Return to AudienceOwn</Link></div></main>}
