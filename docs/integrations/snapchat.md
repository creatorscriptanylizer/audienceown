# Snapchat
Configure Login Kit with `SNAPCHAT_*` and `/api/integrations/snapchat/callback`.
Login Kit supports identity connection; it does not expose general creator public
content reading. Automatic detection is therefore unavailable and the supported
fallback is validated manual import. No scraping is performed.
