# LinkedIn access and review

OpenID identity is separate from restricted social products. `LINKEDIN_REVIEW_STATUS` defaults to `not_configured`; member and organization reads default disabled. Enabling environment flags does not bypass stored review, granted scopes, or authority checks. Email, connection lists, messages, contacts, and arbitrary member profiles are not stored.
