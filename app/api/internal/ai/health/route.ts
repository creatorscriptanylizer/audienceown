import { requiredQueryResults } from "@/lib/api-unavailable";
import { isDeliveryWorkerAuthorized } from "@/lib/delivery-worker-auth";
import { createAdminClient } from "@/lib/supabase/admin";
export async function GET(request:Request){if(!isDeliveryWorkerAuthorized(request.headers.get("authorization"),process.env.AI_WORKER_SECRET))return Response.json({error:"Unauthorized"},{status:401});const db=createAdminClient();if(!db)return Response.json({error:"Not configured"},{status:503});const now=new Date().toISOString(),month=new Date();month.setUTCDate(1);month.setUTCHours(0,0,0,0);const checked=await requiredQueryResults("internal_ai_health",["ai_jobs","ai_usage"],Promise.all([db.from("ai_draft_enhancement_jobs").select("status,lease_expires_at,created_at,last_error_code,completed_at").order("created_at",{ascending:false}).limit(1000),db.from("ai_usage_events").select("estimated_cost_minor_units,success,created_at").gte("created_at",month.toISOString())]));if(checked.response)return checked.response;const[{data:jobs},{data:usage}]=checked.results;return Response.json({pending:(jobs??[]).filter(j=>j.status==="pending").length,processing:(jobs??[]).filter(j=>j.status==="processing").length,expiredLeases:(jobs??[]).filter(j=>j.status==="processing"&&j.lease_expires_at&&j.lease_expires_at<now).length,oldestPending:(jobs??[]).filter(j=>j.status==="pending").at(-1)?.created_at??null,recentFailures:(jobs??[]).filter(j=>j.last_error_code).slice(0,20).map(j=>j.last_error_code),currentMonthUsage:(usage??[]).length,estimatedCostMinorUnits:(usage??[]).reduce((sum,row)=>sum+(row.estimated_cost_minor_units??0),0),lastSuccessfulEnhancement:(jobs??[]).find(j=>j.status==="completed")?.completed_at??null},{headers:{"cache-control":"no-store"}});}
