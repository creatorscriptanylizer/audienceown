import { emergencyApiContext, emergencyError } from "@/lib/emergency/api";
import { planPatchSchema } from "@/lib/emergency/preparedness-schemas";

export async function GET(_request:Request,{params}:{params:Promise<{id:string}>}){const{id}=await params,ctx=await emergencyApiContext();if(!ctx)return Response.json({error:"Unauthorized"},{status:401});const{data,error}=await ctx.client.from("emergency_plans").select("*,emergency_templates(name)").eq("id",id).eq("creator_id",ctx.creator.id).maybeSingle();return error?emergencyError(error):data?Response.json({plan:data}):Response.json({error:"Plan not found"},{status:404});}
export async function PATCH(request:Request,{params}:{params:Promise<{id:string}>}){const{id}=await params,ctx=await emergencyApiContext();if(!ctx)return Response.json({error:"Unauthorized"},{status:401});let body;try{body=await request.json();}catch{return Response.json({error:"Invalid JSON"},{status:400});}const parsed=planPatchSchema.safeParse(body);if(!parsed.success)return Response.json({error:"Invalid plan",fields:parsed.error.flatten().fieldErrors},{status:400});const{data,error}=await ctx.client.from("emergency_plans").update(parsed.data).eq("id",id).eq("creator_id",ctx.creator.id).select().maybeSingle();return error?emergencyError(error):data?Response.json({plan:data}):Response.json({error:"Plan not found"},{status:404});}
