/* eslint-disable @typescript-eslint/no-unused-expressions -- compact retry counter branch */
import{timingSafeEqual}from"node:crypto";import{createAdminClient}from"@/lib/supabase/admin";import{canonicalize}from"@/lib/authenticity/canonical";import{boundedNetworkFetch,decryptWebhookSecret,webhookSignature}from"@/lib/authenticity/network-security";
function authorized(r:Request){const expected=process.env.AUTHENTICITY_WEBHOOK_WORKER_SECRET,actual=r.headers.get("authorization")?.replace(/^Bearer\s+/i,"");return Boolean(expected&&actual&&expected.length===actual.length&&timingSafeEqual(Buffer.from(expected),Buffer.from(actual)));}
export function webhookRetry(attempt:number,max:number,status?:number){if(attempt>=max)return"failed"as const;if(status&&status>=400&&status<500&&status!==408&&status!==429)return"failed"as const;return"retrying"as const;}
export async function POST(request:Request){if(!authorized(request))return Response.json({error:"Unauthorized"},{status:401});const db=createAdminClient();if(!db)return Response.json({error:"Unavailable"},{status:503});const batch=Math.min(100,Math.max(1,Number(process.env.AUTHENTICITY_WEBHOOK_BATCH_SIZE??20)||20)),owner=crypto.randomUUID(),{data:jobs,error}=await db.rpc("claim_authenticity_network_deliveries",{p_limit:batch,p_lease_owner:owner});if(error)return Response.json({error:"Claim failed"},{status:500});let delivered=0,retrying=0,failed=0;for(const job of jobs??[]){const{data:s}=await db.from("authenticity_network_subscriptions").select("endpoint_url,secret_ciphertext,status,failure_count").eq("id",job.subscription_id).maybeSingle();if(!s||!['active','failing'].includes(s.status)){await db.from("authenticity_network_deliveries").update({status:"cancelled",lease_owner:null,lease_expires_at:null}).eq("id",job.id);continue;}let responseStatus:number|undefined;try{const body=canonicalize(job.payload),timestamp=new Date().toISOString(),secret=decryptWebhookSecret(s.secret_ciphertext),result=await boundedNetworkFetch(new URL(s.endpoint_url),{method:"POST",headers:{"content-type":"application/json","x-audienceown-event-id":job.event_id,"x-audienceown-timestamp":timestamp,"x-audienceown-signature":webhookSignature(secret,job.event_id,timestamp,body)},body},{timeoutMs:Number(process.env.AUTHENTICITY_WEBHOOK_TIMEOUT_MS??5000),maxBytes:Number(process.env.AUTHENTICITY_WEBHOOK_MAX_RESPONSE_BYTES??16384)});responseStatus=result.response.status;if(!result.response.ok)throw new Error("delivery rejected");await db.from("authenticity_network_deliveries").update({status:"delivered",response_status:responseStatus,delivered_at:new Date().toISOString(),lease_owner:null,lease_expires_at:null}).eq("id",job.id);await db.from("authenticity_network_subscriptions").update({status:"active",failure_count:0,last_success_at:new Date().toISOString(),next_attempt_at:null}).eq("id",job.subscription_id);delivered++;}catch{const max=Math.min(20,Math.max(1,Number(process.env.AUTHENTICITY_WEBHOOK_MAX_ATTEMPTS??6)||6)),state=webhookRetry(job.attempt_count,max,responseStatus),next=new Date(Date.now()+Math.min(3_600_000,2**job.attempt_count*30_000)).toISOString();await db.from("authenticity_network_deliveries").update({status:state,response_status:responseStatus??null,next_attempt_at:state==="retrying"?next:null,failed_at:state==="failed"?new Date().toISOString():null,lease_owner:null,lease_expires_at:null}).eq("id",job.id);await db.from("authenticity_network_subscriptions").update({status:state==="failed"?"failing":s.status,failure_count:s.failure_count+1,last_failure_at:new Date().toISOString(),next_attempt_at:state==="retrying"?next:null}).eq("id",job.subscription_id);state==="retrying"?retrying++:failed++;}}return Response.json({claimed:jobs?.length??0,delivered,retrying,failed});}
