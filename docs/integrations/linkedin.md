# LinkedIn
Configure `LINKEDIN_*` and `/api/integrations/linkedin/callback` with three-legged
OAuth (`openid profile`). Post reading is enabled only for products approved for
the application; otherwise the card reports provider-review-required and offers
manual URL import. No page scraping or assumed refresh token is used.
