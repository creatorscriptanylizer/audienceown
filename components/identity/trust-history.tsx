export function TrustHistory({events}:{events:{id:number;event_type:string;previous_state:string|null;new_state:string|null;created_at:string}[]}){return <section className="surface rounded-xl p-5"><p className="eyebrow">Trust history</p><h2 className="mt-1 text-lg font-semibold">Verification transitions</h2><ol className="mt-4 space-y-3">{events.length?events.map(event=><li className="border-l border-white/10 pl-3 text-sm" key={event.id}><strong>{event.event_type.replaceAll("_"," ")}</strong><p className="text-xs text-zinc-400">{event.previous_state??"none"} → {event.new_state??"unchanged"} · {new Date(event.created_at).toLocaleString()}</p></li>):<li className="text-sm text-zinc-400">Trust transitions appear after evaluation.</li>}</ol></section>}
