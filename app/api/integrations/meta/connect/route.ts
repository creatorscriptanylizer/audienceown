import{randomBytes}from"node:crypto";import{cookies}from"next/headers";import{NextResponse}from"next/server";import{getCreator,getViewer}from"@/lib/dal";import{createOAuthState}from"@/lib/social-providers/oauth";import{graphVersion}from"@/lib/providers/meta";
export async function GET(request:Request){const[user,creator]=await Promise.all([getViewer(),getCreator()]);if(!user||!creator)return NextResponse.redirect(new URL("/login?next=/dashboard/platforms",request.url));const appId=process.env.META_APP_ID,redirect=process.env.META_REDIRECT_URI;if(!appId||!redirect)return NextResponse.redirect(new URL("/dashboard/platforms?meta=configuration_pending",request.url));const nonce=randomBytes(24).toString("base64url"),state=createOAuthState({creatorId:creator.id,userId:user.id,provider:"facebook",nonce,expiresAt:Date.now()+600000}),store=await cookies();store.set("meta_oauth",JSON.stringify({nonce}),{httpOnly:true,secure:process.env.NODE_ENV==="production",sameSite:"lax",path:"/api/integrations/meta/callback",maxAge:600});const query=new URLSearchParams({client_id:appId,redirect_uri:redirect,response_type:"code",scope:"pages_show_list,pages_read_engagement,pages_read_user_content,instagram_basic",state});return NextResponse.redirect(`https://www.facebook.com/${graphVersion()}/dialog/oauth?${query}`);}
