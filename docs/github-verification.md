# GitHub verification

GitHub users, organizations, public repositories, and supported packages use numeric GitHub IDs rather than usernames. Organizations require owner/admin authority; repositories require admin or verified organization authority. Renames preserve identity while transfers, deletion, archival, installation removal, and private visibility trigger revalidation or suppression.

Private repositories and repository content are never public. Contributions, stars, and follower counts do not affect authenticity.

