import"server-only";import{createAdminClient}from"@/lib/supabase/admin";import{parsePublicEcosystemGraph}from"./public";
export async function getPublicEcosystem(slug:string){const db=createAdminClient();if(!db)return null;const[{data,error},{data:creator}]=await Promise.all([db.rpc("get_public_creator_ecosystem_graph",{p_slug:slug}),db.from("creators").select("id").eq("public_slug",slug).maybeSingle()]);if(error)return null;const graph=parsePublicEcosystemGraph(data);if(!graph||!creator)return graph;const{data:manual}=await db.from("manual_service_connections").select("service_name,service_category,canonical_url,public_handle,last_verified_at,primary_for_category").eq("creator_id",creator.id).eq("verification_status","verified").eq("official",true).eq("public_visible",true).is("revoked_at",null).is("archived_at",null);for(const service of manual??[])graph.destinations.push({type:["community","newsletter","membership","commerce","booking","website","application","podcast"].includes(service.service_category)?service.service_category as "community"|"newsletter"|"membership"|"commerce"|"booking"|"website"|"application"|"podcast":"other",provider:"manual_service",name:service.service_name,handle:service.public_handle??undefined,url:service.canonical_url,official:true,primary:service.primary_for_category,verifiedAt:service.last_verified_at??undefined});return graph;}
