import{NextResponse}from"next/server";import{createClient as createAdmin}from"@supabase/supabase-js";import{createClient}from"@/lib/supabase/server";import type{Database}from"@/lib/database.types";import{buildAudienceExportCsv}from"@/lib/audience-export";export async function GET(){const s=await createClient();if(!s)return NextResponse.json({error:"Not configured"},{status:503});const{data:{user}}=await s.auth.getUser();if(!user)return NextResponse.json({error:"Unauthorized"},{status:401});const{data:creator,error:creatorError}=await s.from("creators").select("id,public_slug").eq("owner_user_id",user.id).maybeSingle();if(creatorError)return NextResponse.json({error:"Temporarily unavailable"},{status:503,headers:{"cache-control":"no-store"}});if(!creator)return NextResponse.json({error:"Creator not found"},{status:404});const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.SUPABASE_ADMIN_KEY||process.env.SUPABASE_SERVICE_ROLE_KEY||process.env.SUPABASE_SECRET_KEY;if(!url||!key)return NextResponse.json({error:"Export is not configured"},{status:503});const admin=createAdmin<Database>(url,key,{auth:{persistSession:false}});const{data:connections,error:connectionsError}=await admin.from("follower_connections").select("id,follower_contact_id,consented_at,activated_at,source_platform").eq("creator_id",creator.id).eq("status","active").order("consented_at");if(connectionsError)return NextResponse.json({error:"Temporarily unavailable"},{status:503,headers:{"cache-control":"no-store"}});const contactIds=[...new Set((connections??[]).map(x=>x.follower_contact_id))],connectionIds=(connections??[]).map(x=>x.id);const[{data:contacts,error:contactsError},{data:prefs,error:prefsError}]=await Promise.all([contactIds.length?admin.from("follower_contacts").select("id,email_masked,phone_masked").in("id",contactIds):Promise.resolve({data:[],error:null}),connectionIds.length?admin.from("follower_notification_preferences").select("follower_connection_id,creator_announcements,new_content,important_account_updates").in("follower_connection_id",connectionIds):Promise.resolve({data:[],error:null})]);if(contactsError||prefsError)return NextResponse.json({error:"Temporarily unavailable"},{status:503,headers:{"cache-control":"no-store"}});const csv=buildAudienceExportCsv(connections??[],contacts??[],prefs??[]);return new NextResponse(csv,{headers:{"content-type":"text/csv; charset=utf-8","content-disposition":`attachment;filename="audienceown-${creator.public_slug}-protected-audience.csv"`,"cache-control":"private, no-store, max-age=0","x-robots-tag":"noindex, nofollow, noarchive"}});}
