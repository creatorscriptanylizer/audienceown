import{getCreator}from"@/lib/dal";import{checkManualServiceChallenge}from"@/lib/providers/provider-expansion-four-server";
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){const c=await getCreator(),{id}=await params;if(!c)return Response.json({error:"Unauthorized"},{status:401});const origin=request.headers.get("origin");if(origin&&origin!==new URL(request.url).origin)return Response.json({error:"Forbidden"},{status:403});try{return Response.json(await checkManualServiceChallenge(c.id,id));}catch{return Response.json({error:"Verification check failed"},{status:409});}}
