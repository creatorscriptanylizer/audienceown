# Threads
Configure `THREADS_*` and `/api/integrations/threads/callback`. Request
`threads_basic` and complete Meta review. Threads remains its own adapter.
Approved text, image, video, carousel, permalink, and timestamp objects normalize
through polling. Missing permissions leave the provider review-required.
