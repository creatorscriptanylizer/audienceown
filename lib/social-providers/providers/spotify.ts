import{capabilities}from"../capabilities";import{oauthProvider}from"../provider-factory";import{classifyExpansionFourError,expansionFourAccess,manualExpansionFourContent}from"@/lib/providers/provider-expansion-four";import{exchangeSpotifyCode,fetchSpotifyIdentity,refreshSpotifyToken}from"@/lib/providers/spotify";
const scopes=["user-read-private"];const configured=()=>Boolean(process.env.SPOTIFY_CLIENT_ID&&process.env.SPOTIFY_CLIENT_SECRET&&process.env.SPOTIFY_REDIRECT_URI);
export const spotifyProvider=oauthProvider({displayName:"Spotify",availability:"implemented_credentials_required",unavailableReason:"Spotify login proves only the user account; it does not verify artist or show authority.",capabilities:capabilities({oauth:true,tokenRefresh:true,automaticPublishing:false,manualImport:true}),config:{provider:"spotify",clientIdEnv:"SPOTIFY_CLIENT_ID",clientSecretEnv:"SPOTIFY_CLIENT_SECRET",redirectEnv:"SPOTIFY_REDIRECT_URI",authorizeUrl:"https://accounts.spotify.com/authorize",tokenUrl:"https://accounts.spotify.com/api/token",scopes,pkce:true},adapter:{exchangeAuthorizationCode:({code,codeVerifier})=>exchangeSpotifyCode(code,codeVerifier),refreshAccessToken:({refreshToken})=>{if(!refreshToken)throw new Error("token_refresh_failed");return refreshSpotifyToken(refreshToken);},fetchIdentity:({accessToken})=>fetchSpotifyIdentity(accessToken),readiness:()=>{const credentialsPresent=configured(),asset=expansionFourAccess(process.env.SPOTIFY_CREATOR_ASSET_ACCESS_STATUS);return{configured:credentialsPresent,credentialsPresent,productConfigured:credentialsPresent,requiredScopes:scopes,grantedScopes:[],missingScopes:[],accessLevel:credentialsPresent?"development":"none",accessTier:asset,reviewStatus:"not_configured",connectionAvailable:credentialsPresent,identityAvailable:credentialsPresent,assetDiscoveryAvailable:false,contentDetectionAvailable:false,pollingAvailable:false,webhookAvailable:false,streamAvailable:false,manualImportAvailable:true,manualVerificationAvailable:true,verificationAvailable:true,manualFallbackAvailable:true,missingConfiguration:["SPOTIFY_CLIENT_ID","SPOTIFY_CLIENT_SECRET","SPOTIFY_REDIRECT_URI"].filter(n=>!process.env[n]),limitations:["Spotify login verifies only the Spotify user profile, not artist or show ownership.","The Web API does not enumerate creator-owned artists or shows, so asset selection is unavailable.","Audience metrics are unavailable for the connected user identity; deprecated user followers are not used.","Artist releases remain manual-only and shows require verified feed linkage.","Spotify publishing is disabled."]};},manualImport:async v=>manualExpansionFourContent("spotify",v),manualVerification:async()=>({verified:false,reason:"A Spotify URL or login alone does not verify artist/show ownership."}),classifyError:classifyExpansionFourError}});
