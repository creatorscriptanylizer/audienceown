import"server-only";import{z}from"zod";import{createAdminClient}from"@/lib/supabase/admin";import{decryptSocialSecret,encryptSocialSecret}from"@/lib/social-secrets";import{discoverMetaPages,fetchInstagramIdentity}from"./meta";
const meSchema=z.object({id:z.string().min(1),name:z.string().min(1)});
async function connectionSecret(creatorId:string,connectionId:string){const db=createAdminClient();if(!db)throw new Error("provider_not_configured");const[{data:connection},{data:secret}]=await Promise.all([db.from("connected_accounts").select("id,creator_id,platform,granted_scopes").eq("id",connectionId).eq("creator_id",creatorId).eq("platform","facebook").single(),db.from("platform_connection_secrets").select("access_token_ciphertext").eq("platform_connection_id",connectionId).single()]);if(!connection||!secret)throw new Error("connection_unavailable");return{db,token:decryptSocialSecret(secret.access_token_ciphertext),grantedScopes:connection.granted_scopes};}
export async function fetchMetaAppUser(accessToken:string){const response=await fetch("https://graph.facebook.com/me?fields=id,name",{headers:{authorization:`Bearer ${accessToken}`},cache:"no-store"});const value:unknown=await response.json();if(!response.ok)throw new Error("authentication_revoked");return meSchema.parse(value);}
export async function listMetaAssets(creatorId:string,connectionId:string){const{token}=await connectionSecret(creatorId,connectionId),pages=await discoverMetaPages(token);return pages.flatMap(page=>[{provider:"facebook"as const,assetType:"facebook_page",displayName:page.displayName,canonicalUrl:page.canonicalUrl,selectionKey:page.stableSourceId,authorityAvailable:true,linkedInstagramAvailable:Boolean(page.instagramId)},...(page.instagramId?[{provider:"instagram"as const,assetType:"instagram_professional_account",displayName:`Instagram linked to ${page.displayName}`,canonicalUrl:"https://www.instagram.com/",selectionKey:`${page.stableSourceId}:${page.instagramId}`,authorityAvailable:true,linkedInstagramAvailable:true}]:[])]);}
export async function selectMetaAsset(creatorId:string,connectionId:string,provider:"facebook"|"instagram",selectionKey:string){const{db,token,grantedScopes}=await connectionSecret(creatorId,connectionId),pages=await discoverMetaPages(token),[pageId,instagramId]=selectionKey.split(":"),page=pages.find(candidate=>candidate.stableSourceId===pageId);if(!page)throw new Error("page_not_managed");const credential=page.pageToken,credentialType="page_access";let stableId=page.stableSourceId,displayName=page.displayName??"Facebook Page",displayHandle=typeof page.metadata.username==="string"?page.metadata.username:null,canonicalUrl=page.canonicalUrl,parentId:string|null=null,assetType="facebook_page";
 if(provider==="instagram"){if(!instagramId||page.instagramId!==instagramId)throw new Error("account_not_professional");const identity=await fetchInstagramIdentity(page.pageToken,instagramId);stableId=identity.id;displayName=identity.name;displayHandle=typeof identity.metadata.handle==="string"?identity.metadata.handle:null;canonicalUrl=identity.url;parentId=page.stableSourceId;assetType="instagram_professional_account";}
 const existing=await db.from("connected_accounts").select("id").eq("creator_id",creatorId).eq("platform",provider).eq("external_account_id",stableId).maybeSingle(),required=provider==="facebook"?["pages_show_list","pages_read_engagement","pages_read_user_content"]:["pages_show_list","instagram_basic"],values={creator_id:creatorId,platform:provider,account_type:"official"as const,label:displayName,url:canonicalUrl,is_primary:false,is_public:true,external_account_id:stableId,external_account_name:displayName,external_account_url:canonicalUrl,provider_metadata:{loginMethod:"facebook_login",parentMetaConnectionId:connectionId,parentAssetId:parentId,tokenType:credentialType,pageTasks:page.tasks},requested_scopes:required,granted_scopes:grantedScopes.filter(scope=>required.includes(scope)),watch_enabled:required.every(scope=>grantedScopes.includes(scope)),auto_create_drafts:true,auto_send:false,webhook_enabled:Boolean(process.env.META_WEBHOOK_VERIFY_TOKEN),connection_health:"healthy",provider_status:required.every(scope=>grantedScopes.includes(scope))?"ready":"automatic_detection_unavailable"},selected=existing.data?await db.from("connected_accounts").update(values).eq("id",existing.data.id).select("id").single():await db.from("connected_accounts").insert(values).select("id").single();if(selected.error)throw selected.error;
 await db.from("platform_connection_secrets").upsert({platform_connection_id:selected.data.id,access_token_ciphertext:encryptSocialSecret(credential),refresh_token_ciphertext:null,token_scope:(provider==="facebook"?page.tasks:["instagram_basic"]).join(" "),token_type:credentialType});
 const{data:assetId,error}=await db.rpc("select_provider_asset",{p_connected_account_id:selected.data.id,p_provider:provider,p_asset_type:assetType,p_stable_asset_id:stableId,p_parent_asset_id:parentId??"",p_display_name:displayName,p_display_handle:displayHandle??"",p_canonical_url:canonicalUrl,p_metadata:{loginMethod:"facebook_login",tokenType:credentialType,authorityValidated:true}});if(error||!assetId)throw error??new Error("asset_selection_failed");
 const secret=await db.from("provider_asset_secrets").upsert({asset_binding_id:assetId,creator_id:creatorId,provider,credential_ciphertext:encryptSocialSecret(credential),credential_type:credentialType,granted_permissions:page.tasks},{onConflict:"asset_binding_id"});if(secret.error)throw secret.error;if(process.env.META_WEBHOOK_VERIFY_TOKEN)await db.from("meta_webhook_subscriptions").upsert({creator_id:creatorId,asset_binding_id:assetId,object_type:provider==="facebook"?"page":"instagram",subscribed_fields:provider==="facebook"?["feed","name","permissions"]:["media","account_update"],status:"pending",next_reconcile_at:new Date().toISOString()},{onConflict:"asset_binding_id,object_type"});await db.rpc("sync_identity_account_from_connection",{p_connection_id:selected.data.id});return{id:assetId,provider,displayName,displayHandle,canonicalUrl,detectionReady:required.every(scope=>grantedScopes.includes(scope))};}
