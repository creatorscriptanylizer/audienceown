import type{ProviderReadiness,SocialProviderAdapter}from"./types";
const envByProvider:Record<string,string[]>={twitch:["TWITCH_CLIENT_ID","TWITCH_CLIENT_SECRET","TWITCH_REDIRECT_URI"],discord:["DISCORD_CLIENT_ID","DISCORD_CLIENT_SECRET","DISCORD_REDIRECT_URI"],podcast:[],rss:[]};
export function providerReadiness(adapter:SocialProviderAdapter):ProviderReadiness{if(adapter.readiness)return adapter.readiness();const required=envByProvider[adapter.provider]??[],missingConfiguration=required.filter(name=>!process.env[name]);return{configured:missingConfiguration.length===0,connectionAvailable:adapter.capabilities.oauth&&missingConfiguration.length===0,verificationAvailable:Boolean(adapter.fetchIdentity||adapter.verifyOwnership||adapter.manualVerification),contentDetectionAvailable:adapter.capabilities.contentDetection,webhookAvailable:adapter.capabilities.webhooks,pollingAvailable:adapter.capabilities.polling,manualFallbackAvailable:adapter.capabilities.manualImport,missingConfiguration,limitations:[adapter.unavailableReason].filter((x):x is string=>Boolean(x))};}
