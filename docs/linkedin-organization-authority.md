# LinkedIn organization authority

Organization discovery uses approved organization ACL APIs and retains only roles sufficient for content administration. Roles remain private. A creator must explicitly select each organization; the selected organization receives its own asset binding, provider connection, encrypted credential association, and source. Losing the role suppresses detection and official presentation.
