import{ShieldCheck,TriangleAlert,Waypoints}from"lucide-react";
export function IdentityOverviewCard({status,total,verified,attention,providers}:{status:string;total:number;verified:number;attention:number;providers:number}){return <section className="surface rounded-xl p-5"><div className="flex items-center gap-3"><span className="rounded-lg bg-emerald-400/10 p-2 text-emerald-300"><Waypoints size={22}/></span><div><p className="eyebrow">Canonical identity</p><h2 className="mt-1 text-xl font-semibold">{status.replaceAll("_"," ")}</h2></div></div><div className="mt-5 grid grid-cols-2 gap-3 md:grid-cols-4">{[["Accounts",total],["Verified",verified],["Providers",providers],["Needs attention",attention]].map(([label,value])=><div className="rounded-lg border border-white/10 p-3" key={label}><strong className="text-xl">{value}</strong><p className="text-xs text-zinc-400">{label}</p></div>)}</div><p className="mt-4 flex items-center gap-2 text-xs text-zinc-400">{attention?<TriangleAlert size={14}/>:<ShieldCheck size={14}/>} Stable provider IDs anchor this graph; handles are display information.</p></section>}
