# Spotify
Configure `SPOTIFY_*` and `/api/integrations/spotify/callback`. Select and validate
an artist before release polling. Albums, singles, compilations, and authorized
show episodes normalize as releases. Preserve Spotify links/artwork attribution;
never download or rehost audio. Refresh-token rotation is supported.
