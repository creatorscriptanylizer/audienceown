"use client";
import { useEffect } from "react";
import { CircleAlert } from "lucide-react";
export default function ErrorPage({error,reset}:{error:Error&{digest?:string};reset:()=>void}){useEffect(()=>{console.error(JSON.stringify({level:"error",event:"ui.error",digest:error.digest}))},[error]);return <main className="grid min-h-screen place-items-center p-5 text-center"><section role="alert" className="surface w-full max-w-xl rounded-2xl p-7"><span className="mx-auto grid size-11 place-items-center rounded-xl bg-red-400/10 text-red-300"><CircleAlert aria-hidden size={20}/></span><h1 className="mt-4 text-2xl font-semibold">We could not load this page</h1><p className="mt-2 text-zinc-400">We could not reach the service. Nothing was changed, and it is safe to try again.</p><button type="button" onClick={reset} className="button button-primary mt-6">Try again</button></section></main>}
