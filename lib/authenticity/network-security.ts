import"server-only";import{createCipheriv,createDecipheriv,createHash,createHmac,randomBytes,timingSafeEqual}from"node:crypto";import{assertPublicDomain}from"@/lib/emergency/domain-verification";
function secretKey(){const value=process.env.AUTHENTICITY_WEBHOOK_WORKER_SECRET;if(!value)throw new Error("webhook_encryption_unavailable");return createHash("sha256").update(value).digest();}
export function encryptWebhookSecret(value:string){const iv=randomBytes(12),cipher=createCipheriv("aes-256-gcm",secretKey(),iv),body=Buffer.concat([cipher.update(value,"utf8"),cipher.final()]);return Buffer.concat([iv,cipher.getAuthTag(),body]).toString("base64");}
export function decryptWebhookSecret(value:string){const data=Buffer.from(value,"base64"),decipher=createDecipheriv("aes-256-gcm",secretKey(),data.subarray(0,12));decipher.setAuthTag(data.subarray(12,28));return Buffer.concat([decipher.update(data.subarray(28)),decipher.final()]).toString("utf8");}
export function webhookSignature(secret:string,eventId:string,timestamp:string,body:string){return createHmac("sha256",secret).update(`${eventId}.${timestamp}.${body}`).digest("base64url");}
export function verifyWebhookSignature(secret:string,eventId:string,timestamp:string,body:string,signature:string,now=Date.now()){if(Math.abs(now-Date.parse(timestamp))>300_000)return false;const expected=webhookSignature(secret,eventId,timestamp,body);return expected.length===signature.length&&timingSafeEqual(Buffer.from(expected),Buffer.from(signature));}
export async function validateNetworkDestination(value:string){const url=new URL(value);if(url.protocol!=="https:"||url.username||url.password||url.hash)throw new Error("unsafe_endpoint");await assertPublicDomain(url.hostname);return url;}
export async function boundedNetworkFetch(url:URL,init:RequestInit,options:{timeoutMs:number;maxBytes:number}){await validateNetworkDestination(url.toString());const response=await fetch(url,{...init,redirect:"manual",signal:AbortSignal.timeout(options.timeoutMs)});if(response.status>=300&&response.status<400)throw new Error("redirect_rejected");const length=Number(response.headers.get("content-length")??0);if(length>options.maxBytes)throw new Error("response_too_large");const body=await response.text();if(Buffer.byteLength(body)>options.maxBytes)throw new Error("response_too_large");return{response,body};}
