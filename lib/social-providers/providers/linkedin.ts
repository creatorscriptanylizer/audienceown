import{capabilities}from"../capabilities";import{oauthProvider}from"../provider-factory";import{classifyExpansionThreeError,expansionReview,manualExpansionThreeContent}from"@/lib/providers/provider-expansion-three";import{exchangeLinkedInCode,fetchLinkedInIdentity}from"@/lib/providers/linkedin";
const scopes=["openid","profile"],configured=()=>Boolean(process.env.LINKEDIN_CLIENT_ID&&process.env.LINKEDIN_CLIENT_SECRET&&process.env.LINKEDIN_REDIRECT_URI);
export const linkedinProvider=oauthProvider({displayName:"LinkedIn",availability:"implemented_credentials_required",unavailableReason:"LinkedIn member OIDC does not provide audience or organization capabilities.",capabilities:capabilities({oauth:true,automaticPublishing:false,manualImport:true}),config:{provider:"linkedin",clientIdEnv:"LINKEDIN_CLIENT_ID",clientSecretEnv:"LINKEDIN_CLIENT_SECRET",redirectEnv:"LINKEDIN_REDIRECT_URI",authorizeUrl:"https://www.linkedin.com/oauth/v2/authorization",tokenUrl:"https://www.linkedin.com/oauth/v2/accessToken",scopes},adapter:{exchangeAuthorizationCode:({code})=>exchangeLinkedInCode(code),fetchIdentity:({accessToken})=>fetchLinkedInIdentity(accessToken),readiness:()=>{const credentialsPresent=configured(),reviewStatus=expansionReview(process.env.LINKEDIN_REVIEW_STATUS);return{configured:credentialsPresent,credentialsPresent,productConfigured:credentialsPresent,requiredScopes:scopes,grantedScopes:[],missingScopes:[],accessLevel:credentialsPresent?"standard":"none",accessTier:"openid_only",reviewStatus,connectionAvailable:credentialsPresent,identityAvailable:credentialsPresent,assetDiscoveryAvailable:false,contentDetectionAvailable:false,webhookAvailable:false,streamAvailable:false,pollingAvailable:false,manualImportAvailable:true,manualVerificationAvailable:true,verificationAvailable:true,manualFallbackAvailable:true,missingConfiguration:["LINKEDIN_CLIENT_ID","LINKEDIN_CLIENT_SECRET","LINKEDIN_REDIRECT_URI"].filter(name=>!process.env[name]),limitations:["OIDC proves control of the authenticated member session, not LinkedIn real-world identity verification.","Personal profile follower and connection counts are unavailable through this product.","Programmatic refresh tokens are available only to approved partners; this connection requires OAuth reconnect at expiry.","LinkedIn provides no general revocation endpoint for this flow; disconnect removes local authorization data.","LinkedIn publishing is disabled."]};},manualImport:async value=>manualExpansionThreeContent("linkedin",value),manualVerification:async()=>({verified:false,reason:"OIDC or a LinkedIn URL alone does not verify real-world identity."}),classifyError:classifyExpansionThreeError}});
