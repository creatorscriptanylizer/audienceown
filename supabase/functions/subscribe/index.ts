import { createClient } from "npm:@supabase/supabase-js@2";
import { z } from "npm:zod@4";
import { cors, json, sha256, token } from "../_shared/http.ts";
const sources=["tiktok","instagram","youtube","x","facebook","threads","snapchat","twitch","linkedin","spotify","discord","pinterest","website","direct","other"] as const;
const schema=z.object({slug:z.string().regex(/^[a-z0-9]+(?:-[a-z0-9]+)*$/),email:z.string().email().transform(v=>v.trim().toLowerCase().normalize("NFKC")),source_platform:z.enum(sources).default("direct"),source_referrer:z.string().url().max(500).nullable().optional(),landing_path:z.string().max(300).optional(),preferences:z.object({creator_announcements:z.boolean(),new_content:z.boolean(),important_account_updates:z.boolean()})});
async function encrypt(value:string,secret:string){const iv=crypto.getRandomValues(new Uint8Array(12)),key=await crypto.subtle.importKey("raw",await crypto.subtle.digest("SHA-256",new TextEncoder().encode(secret)),"AES-GCM",false,["encrypt"]),cipher=new Uint8Array(await crypto.subtle.encrypt({name:"AES-GCM",iv},key,new TextEncoder().encode(value))),out=new Uint8Array(iv.length+cipher.length);out.set(iv);out.set(cipher,iv.length);let raw="";out.forEach(b=>raw+=String.fromCharCode(b));return btoa(raw)}
Deno.serve(async(req)=>{if(req.method==="OPTIONS")return new Response(null,{headers:cors});if(req.method!=="POST")return json({error:"Method not allowed"},405);try{const input=schema.parse(await req.json()),admin=createClient(Deno.env.get("SUPABASE_URL")!,Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")!),secret=Deno.env.get("CONTACT_ENCRYPTION_KEY");if(!secret)return json({error:"Not configured"},503);const{data:creator}=await admin.from("creators").select("id,recovery_pass_enabled").eq("public_slug",input.slug).eq("public_profile_enabled",true).maybeSingle();if(!creator?.recovery_pass_enabled)return json({error:"Not found"},404);const emailHash=await sha256(input.email),masked=input.email.replace(/^(.).+(@.+)$/,"$1••••$2"),cipher=await encrypt(input.email,secret);const{data:contact,error:contactError}=await admin.from("follower_contacts").upsert({email_hash:emailHash,email_ciphertext:cipher,email_masked:masked},{onConflict:"email_hash"}).select("id").single();if(contactError)throw contactError;const preferenceToken=token(),unsubscribeToken=token(),now=new Date().toISOString();const{data:existing}=await admin.from("follower_connections").select("id,source_platform").eq("creator_id",creator.id).eq("follower_contact_id",contact.id).maybeSingle();const payload={creator_id:creator.id,follower_contact_id:contact.id,status:"active",consented_at:now,activated_at:now,deactivated_at:null,unsubscribed_at:null,preference_token_hash:await sha256(preferenceToken),unsubscribe_token_hash:await sha256(unsubscribeToken),consent_source:"creator_recovery_pass",...(existing?{}:{source_platform:input.source_platform,source_referrer:input.source_referrer??null,landing_path:input.landing_path??null})};const{data:connection,error}=await admin.from("follower_connections").upsert(payload,{onConflict:"creator_id,follower_contact_id"}).select("id").single();if(error)throw error;await Promise.all([admin.from("follower_recovery_methods").upsert({follower_contact_id:contact.id,method_type:"email",method_status:"verified",destination_hash:emailHash,destination_masked:masked,verified_at:now,consented_at:now},{onConflict:"follower_contact_id,method_type,destination_hash"}),admin.from("follower_notification_preferences").upsert({follower_connection_id:connection.id,...input.preferences})]);return json({status:"activated",preferenceToken,unsubscribeToken})}catch{return json({error:"Recovery Pass could not be activated"},400)}});
