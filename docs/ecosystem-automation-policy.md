# Ecosystem automation policy

Policy version: `ecosystem-automation-v1`. It separates harmless metadata, safe canonical continuity, transient/repeated failure, missing scope/permission, revocation, stable identity conflict, transfer, privacy/deletion, manifest drift, feed migration, expiry, and recovery. Stable-ID mismatches never auto-apply. Non-authoritative signals cannot suppress verification, and emergency presentation retains priority.
