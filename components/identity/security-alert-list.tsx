export function SecurityAlertList({alerts}:{alerts:{id:string;severity:string;title:string;message:string;read_at:string|null;dismissed_at:string|null;created_at:string}[]}){const visible=alerts.filter(a=>!a.dismissed_at);return <section className="surface rounded-xl p-5"><p className="eyebrow">Private security alerts</p><h2 className="mt-1 text-lg font-semibold">Creator alerts</h2><div className="mt-4 space-y-3">{visible.length?visible.map(a=><article className="rounded-lg border border-white/10 p-3" key={a.id}><small className="uppercase text-zinc-500">{a.severity}{a.read_at?" · read":" · new"}</small><strong className="mt-1 block">{a.title}</strong><p className="text-xs text-zinc-400">{a.message}</p></article>):<p className="text-sm text-zinc-400">No pending security alerts.</p>}</div></section>}
