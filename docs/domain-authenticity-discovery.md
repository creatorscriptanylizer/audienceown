# Domain authenticity discovery

A verified creator-owned domain may publish `https://DOMAIN/.well-known/audienceown.json` using the dashboard JSON. AudienceOwn requires an exact host, HTTPS, public DNS addresses, no redirects, bounded bytes, a strict timeout, and exact issuer, creator slug, and verification URL. This proves only domain linkage and cannot upgrade provider verification.
