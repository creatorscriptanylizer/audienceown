"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";

const CONFIRMATION="DELETE AUDIENCEOWN";
export function AccountDeletionForm(){const router=useRouter();const[value,setValue]=useState("");const[busy,setBusy]=useState(false);const[message,setMessage]=useState<string|null>(null);
 async function submit(event:React.FormEvent){event.preventDefault();setBusy(true);setMessage(null);try{const response=await fetch("/api/account/delete",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({confirmation:value})});const result=await response.json().catch(()=>({})) as {status?:string;error?:string};if(!response.ok){setMessage(result.error??"Account deletion did not complete. Your account remains available; please retry.");return;}router.replace("/?account=deleted");router.refresh();}catch{setMessage("Account deletion could not be requested. Check your connection and try again.");}finally{setBusy(false);}}
 return <form onSubmit={submit} className="mt-6 max-w-xl" aria-describedby="account-deletion-description"><label className="label" htmlFor="account-delete-confirmation">Type <strong>{CONFIRMATION}</strong> to confirm</label><input id="account-delete-confirmation" className="mt-2 min-h-12 w-full rounded-lg border border-white/10 bg-black/30 px-4 py-3 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-red-400" value={value} onChange={e=>setValue(e.target.value)} autoComplete="off" aria-invalid={Boolean(message)} aria-describedby={message?"account-delete-error":undefined} disabled={busy}/><button className="button mt-4 min-h-11 w-full justify-center bg-red-600 text-white hover:bg-red-500 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-4 focus-visible:outline-red-400 sm:w-auto" disabled={busy||value!==CONFIRMATION}>{busy?"Deleting safely…":"Delete AudienceOwn account"}</button>{message&&<p id="account-delete-error" role="alert" className="mt-4 rounded-lg border border-amber-400/20 bg-amber-400/5 p-3 text-sm text-amber-100">{message}</p>}</form>}
