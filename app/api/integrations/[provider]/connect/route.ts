import{randomBytes}from"node:crypto";import{cookies}from"next/headers";import{NextResponse}from"next/server";import{getCreator,getViewer}from"@/lib/dal";import{createOAuthState,createPkce}from"@/lib/social-providers/oauth";import{getSocialProvider}from"@/lib/social-providers/registry";import{isSocialProvider}from"@/lib/social-providers/normalize";import{canCreateProviderConnection}from"@/lib/provider-entitlements";import{oauthOriginCheck}from"@/lib/oauth-origin";import{debugLog}from"@/lib/debug";import{canonicalAccountConnected}from"@/lib/social-providers/connection-health";export const runtime="nodejs";export async function GET(request:Request,{params}:{params:Promise<{provider:string;}>;}){const{provider:raw}=await params;if(!isSocialProvider(raw))return Response.json({error:"Unknown provider"},{status:404});if(!["instagram","tiktok","x","spotify","linkedin","pinterest","twitch","discord","snapchat"].includes(raw))return Response.json({error:"provider_coming_soon"},{status:409});const adapter=getSocialProvider(raw);if(!adapter.capabilities.oauth||!adapter.createAuthorizationUrl)return Response.json({error:"provider_capability_not_supported"},{status:409});let secureOAuthCookie=new URL(request.url).protocol==="https:";if(raw==="instagram"||raw==="tiktok"){const origin=oauthOriginCheck(request);debugLog("oauth",{event:`${raw}_oauth_authorize`,step:"canonical_origin_check",configuredOrigin:origin.configuredOrigin,externalOrigin:origin.externalOrigin,redirectRequired:origin.redirectRequired});if(origin.loopDetected)return Response.json({error:"oauth_canonical_origin_loop"},{status:500});if(origin.redirectTarget)return NextResponse.redirect(origin.redirectTarget);secureOAuthCookie=new URL(origin.externalOrigin).protocol==="https:";}const[user,creator]=await Promise.all([getViewer(),getCreator()]);if(!user||!creator){const next=raw==="instagram"||raw==="tiktok"?`${new URL(request.url).pathname}${new URL(request.url).search}`:"/dashboard/platforms";return NextResponse.redirect(new URL(`/login?next=${encodeURIComponent(next)}`,request.url));}const requestUrl=new URL(request.url),role=requestUrl.searchParams.get("role")==="backup"?"backup":"official",connectionId=requestUrl.searchParams.get("connectionId")??undefined,protectedOfficialAccountId=requestUrl.searchParams.get("protectedOfficialAccountId")??undefined,returnTo=requestUrl.searchParams.get("returnTo")==="onboarding"?"onboarding"as const:undefined;if(connectionId){const db=(await import("@/lib/supabase/admin")).createAdminClient(),owned=db?await db.from("connected_accounts").select("id,account_type,platform").eq("id",connectionId).eq("creator_id",creator.id).maybeSingle():{data:null};if(!owned.data||owned.data.account_type!==role||owned.data.platform!==raw)return Response.json({error:"invalid_connection"},{status:409});}if(role==="backup"&&protectedOfficialAccountId){const db=(await import("@/lib/supabase/admin")).createAdminClient();if(!db)return Response.json({error:"not_configured"},{status:409});const{data:official,error:officialError}=await db.from("connected_accounts").select("id,url,external_account_id,connection_health,provider_status").eq("id",protectedOfficialAccountId).eq("creator_id",creator.id).eq("platform",raw).eq("account_type","official").maybeSingle();if(officialError)return Response.json({error:"Temporarily unavailable"},{status:503,headers:{"cache-control":"no-store"}});if(!official||!canonicalAccountConnected({health:official.connection_health,providerStatus:official.provider_status,hasPublicUrl:Boolean(official.url),hasExternalAccountId:Boolean(official.external_account_id)}))return Response.json({error:"invalid_official_account"},{status:409});}const entitlement=await canCreateProviderConnection(creator.id,role,connectionId?"reconnect":"new_connection",user);if(!entitlement.allowed)return NextResponse.redirect(new URL(returnTo?`/onboarding/accounts?step=${role}&oauth=${raw}:connection_limit_reached`:`/dashboard/platforms?social=${raw}:connection_limit_reached`,request.url));const nonce=randomBytes(24).toString("base64url");const pkce=adapter.pkce?createPkce():null;const state=createOAuthState({creatorId:creator.id,userId:user.id,provider:raw,nonce,role,connectionId,...(protectedOfficialAccountId?{protectedOfficialAccountId}:{}),expiresAt:Date.now()+600000,codeChallenge:pkce?.challenge,returnTo});if(raw==="instagram"||raw==="tiktok")debugLog("oauth",{event:`${raw}_oauth_authorize`,step:"oauth_state_created"});if(raw==="snapchat")debugLog("oauth",{event:"snapchat_oauth_configuration",provider:"snapchat",pkceEnabled:adapter.pkce});const store=await cookies();store.set(`social_oauth_${raw}`,JSON.stringify({nonce,verifier:pkce?.verifier}),{httpOnly:true,secure:secureOAuthCookie,sameSite:"lax",path:`/api/integrations/${raw}/callback`,maxAge:600});try{const authorizationUrl=(await adapter.createAuthorizationUrl({state,codeChallenge:pkce?.challenge})).url;if(raw==="instagram"||raw==="tiktok")debugLog("oauth",{event:`${raw}_oauth_authorize`,step:"provider_authorization_redirect"});return NextResponse.redirect(authorizationUrl);}catch{return NextResponse.redirect(new URL(returnTo?`/onboarding/accounts?step=${role}&oauth=${raw}:configuration_pending`:`/dashboard/platforms?social=${raw}:configuration_pending`,request.url));}}
