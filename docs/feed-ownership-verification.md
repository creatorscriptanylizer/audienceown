# Feed ownership verification

Ownership may be proven by an already verified creator hostname or an expiring token placed in an allowed channel field, temporary item, or supported Podcasting namespace field. Only SHA-256 token hashes are stored. Attempts are bounded, completed challenges cannot replay, and URL entry or podcast-directory presence never self-verifies a feed.
