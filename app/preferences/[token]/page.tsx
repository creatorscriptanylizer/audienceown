"use client";
import { use, useState } from "react";
export default function Page({params}:{params:Promise<{token:string}>}){const{token}=use(params),[status,setStatus]=useState("");async function save(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget),base=process.env.NEXT_PUBLIC_SUPABASE_URL;if(!base)return setStatus("Pass management is unavailable.");const r=await fetch(`${base}/functions/v1/preferences`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({token,creator_announcements:f.get("creator_announcements")==="on",new_content:f.get("new_content")==="on",important_account_updates:f.get("important_account_updates")==="on"})});const data=await r.json();setStatus(data.status==="saved"?"Recovery Pass updated.":data.status==="unsubscribed"?"This Recovery Pass is deactivated. Activate it again from the creator page.":"This private link is invalid.")}return <main className="grid min-h-screen place-items-center px-5"><form onSubmit={save} className="surface w-full max-w-lg rounded-2xl p-7"><p className="eyebrow">Private Recovery Pass</p><h1 className="mt-2 text-2xl font-semibold">Manage Recovery Pass</h1><p className="mt-2 text-sm text-zinc-400">Choose exactly which alerts you receive.</p><div className="my-6 grid gap-3"><label><input type="checkbox" name="important_account_updates" defaultChecked/> Recovery alerts</label><label><input type="checkbox" name="new_content"/> New video alerts</label><label><input type="checkbox" name="creator_announcements"/> Creator announcements</label></div><button className="button button-primary">Save preferences</button>{status&&<p role="status" className="mt-4 text-sm text-zinc-300">{status}</p>}<p className="mt-6 text-xs text-zinc-500">To deactivate your pass, use the private deactivation link provided when it was activated.</p></form></main>}
