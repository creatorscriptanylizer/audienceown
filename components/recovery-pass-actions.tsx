"use client";import{Check,Copy,Share2}from"lucide-react";import{useState}from"react";import{copyRecoveryPassLink,shareRecoveryPassLink}from"@/lib/recovery-pass-client";
export function RecoveryPassActions({url}:{url:string}){const[copied,setCopied]=useState(false);async function copy(){await copyRecoveryPassLink(url);setCopied(true);setTimeout(()=>setCopied(false),1800)}async function share(){const result=await shareRecoveryPassLink(url);if(result==="copied"){setCopied(true);setTimeout(()=>setCopied(false),1800)}}return <div className="recovery-pass-actions"><button type="button" className="button button-secondary" onClick={copy}>{copied?<Check size={16}/>:<Copy size={16}/>} {copied?"Copied":"Copy Recovery Pass"}</button><button type="button" className="button button-secondary" onClick={share}><Share2 size={16}/> Share</button></div>}
