export function ProviderHealthCard({provider,webhookEnabled,failures,lastSuccess}:{provider:string;webhookEnabled:boolean;failures:number;lastSuccess:string|null}){return <div className="surface rounded-xl p-4"><p className="font-medium capitalize">{provider}</p><p className="mt-2 text-xs text-zinc-400">{webhookEnabled?"Webhook + polling fallback":"Polling fallback"} · {failures} failures</p><p className="mt-1 text-xs text-zinc-500">Last success: {lastSuccess?new Date(lastSuccess).toLocaleDateString():"—"}</p></div>}
