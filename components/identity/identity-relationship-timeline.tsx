export function IdentityRelationshipTimeline({relationships}:{relationships:Array<{id:string;relationship_type:string;status:string;created_at:string;source_account_id:string|null;target_account_id:string|null}>}){return <section className="surface rounded-xl p-5"><p className="eyebrow">Continuity</p><h2 className="mt-2 text-lg font-semibold">Replacement and migration history</h2><div className="mt-4 space-y-3">{relationships.length?relationships.map(item=><div className="border-l border-emerald-400/30 pl-3" key={item.id}><strong className="text-sm">{item.relationship_type.replaceAll("_"," ")}</strong><p className="text-xs text-zinc-500">{item.status} · {new Date(item.created_at).toLocaleString()}</p></div>):<p className="text-sm text-zinc-500">No replacement or migration relationships yet.</p>}</div></section>}
